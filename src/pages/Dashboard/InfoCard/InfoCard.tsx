import styled from "styled-components";

const Wrapper = styled.div`
  width: 100%;
  display: flex;
  flex-wrap: wrap;
`;
const InfoCard = () => {
    return (
        <Wrapper>

        </Wrapper>
    );
};

export default InfoCard;